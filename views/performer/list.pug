extends ../layout

block content
  .card-columns
    each item in data
      .card
        .card-block
          h3.card-title
            =item.profile.name
          .card-title
            =item.email
