import { h } from 'preact'
import { connect } from 'preact-redux'

import EditEventForm from '../containers/EventForm'

const EditEvent = () => {
  return (
    <EditEventForm />
  )
}

export default EditEvent
